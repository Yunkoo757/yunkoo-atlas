# 本轮验证

2026-09-20，Windows，Electron 43.1.0。

- 6 项 Node 测试通过：重启持久化、目标封顶、离线结算、末日剩余、时区及 DST、非法输入与时钟回退、残留临时文件、损坏链、失败写入不递增。
- 实际 Electron 交互通过：空状态 → 字段校验 → 创建 → 完成一次 → 关闭重开保留进度 → 全部完成禁用；第二个长名称训练；键盘空格计数；真实文件发布失败与恢复。
- 默认 780×800 与最小 560×620 窗口，实测设备缩放 175%。最小窗口内容宽 547 CSS px，无横向溢出。长名称换行，页面允许单一纵向滚动，主动作可达。
- 实际 computed style：标题 20/28/600，计数 24/28/600，按钮 36px 高、13/20/500，Inter 已加载，主按钮 #5e6ad2。
- 已查看空状态、校验失败、进度、完成并展开历史、最小窗口长名称、写入错误的完整视口截图。错误提示已改至顶部并滚入可见区域。
- 测试使用临时独立目录，未读取或修改 Atlas 资料库。桌面证据位于 test-results/，脚本可重新生成。
- 未验证 macOS、实体中文输入法及其他缩放；未制作安装包或远端发布。

复核窗口通过 `node test/open-demo.mjs` 创建，标题带“隔离测试”，示例 23/100、今日 2/3，历史六个达标日、一个未达标日。测试目录由命令返回并保留，不是正式个人训练数据。

## 紧凑减法版复核

默认窗口改为 420×480，最小 380×440。重新通过实际 Electron 完整交互脚本，175% 缩放下最小视口 368×406 CSS px，无横向溢出；主按钮仍为 36px，未缩小字体掩盖布局。已查看紧凑进度、长名称和并排创建字段的完整视口截图。保留此前隔离测试数据目录并重启最新窗口，未重置示例中的用户试用记录。macOS 未验证。

## 动效复核

实际 Electron 交互回归通过，新增断言：成功计数后存在 220ms 动画、展开记录后再次计数仍保持展开、切换减少动态效果后计数准确且活动动画为零。动画参数证据见 test-results/motion.json；截图在动画完成后采集并查看。尺寸仍为 420×480 / 380×440，未修改存储规则。

## 记录展开稳定性修复

使用与反馈一致的 8 条完成记录复现：原实现展开过程中主按钮宽度从 334.857px 变为 320px，滚动条出现造成约 14.857px 横向重排。固定根滚动槽、使用 8px Token 滚动条并关闭滚动锚定后，逐帧采样宽度变化与左边位置变化均为 0。新增 test/disclosure.mjs，覆盖连续 8 次展开收起、嵌套完成时间、滚到底部后收起、8/100 计数不变；实际 Electron 全流程回归通过。已查看展开明细截图。

## 连点与窗口修复

实际 Electron 双击加 2、零间隔 10 次鼠标点击加 10 测试通过；初次测试复现按钮重建吞点击，改为保留按钮节点后通过。最小化和恢复通过，截图检查同色自定义窗口栏；记录展开稳定性回归仍为宽度变化 0。未在 macOS 实测自定义窗口栏。
