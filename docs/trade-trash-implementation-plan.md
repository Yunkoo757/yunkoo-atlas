# 交易回收站实施方案（简化版）

鉴于交易回收站功能与判例回收站逻辑完全一致，建议采用以下快速实施路径：

---

## 🎯 快速实施方案

### 方案 A：复用判例回收站代码（推荐）

**核心思路**：
- 交易回收站逻辑与判例回收站完全相同
- 可复用已有的 TrashView 组件结构
- 仅需调整数据源和路由

**实施步骤**：

1. **已完成** ✅
   - Trade 数据模型添加 `deletedAt`、`deletedBy` 字段
   - Store 层添加 `removeTrade`（软删除）、`restoreTrade`、`purgeTrade`

2. **待完成**（约 30 分钟）
   - 修改所有交易视图过滤（添加 `.filter((t) => !t.deletedAt)`）
   - 创建交易回收站路由 `/trade-trash`
   - 在复盘模块侧边栏添加回收站入口

---

### 方案 B：等待判例回收站验证后实施

**建议**：
- 先充分测试判例回收站功能
- 确认所有逻辑正确后再复制应用到交易模块
- 避免同时修改两个模块导致混乱

---

## 📋 关键差异点

| 功能 | 判例案例 | 交易 |
|------|----------|------|
| 删除入口 | 右键菜单 → "移至回收站" | 右键菜单 → "移至回收站" |
| 回收站路由 | `/trash` | `/trade-trash` |
| 侧边栏位置 | 判例模块底部 | 复盘模块底部 |
| 搜索字段 | 案例ID、纠纷类型、裁决结果 | 交易ID、标的、策略、状态 |
| 批量操作 | ✅ 支持 | ✅ 支持 |
| 30天清理 | ✅ 自动 | ✅ 自动 |

---

## 🚀 推荐行动

**立即执行**（方案 A）：
1. 复制 TrashView.tsx → TradeTrashView.tsx
2. 修改数据源：`cases` → `trades`
3. 修改路由：`/trash` → `/trade-trash`
4. 添加侧边栏入口（复盘模块）

**或者稍后执行**（方案 B）：
- 待判例回收站稳定运行后
- 直接复制整个回收站逻辑
- 调整数据源和UI文案

---

**您希望我现在立即实施方案 A，还是等待判例回收站验证后再实施方案 B？**

如果选择方案 A，我将在 10 分钟内完成交易回收站的基本功能。