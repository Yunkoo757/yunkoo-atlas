IMPORTANT:



\- Always read and write files as UTF-8

\- Never use GBK / GB2312 / ANSI

\- Preserve all Chinese characters

\- Never convert non-ASCII text

\- Save files as UTF-8 without BOM

请使用简体中文回复我，并且在每次回复前说：Yunkoo老师好，

平台适配范围：本软件后续仅适配 Windows 客户端和 macOS 客户端。后续新增功能、界面调整、技术方案与评估仅面向这两个桌面平台，不再考虑手机端、iPad 端、浏览器端或其他平台的适配与支持。

禁止在本项目的任何后续对话或任务中使用 Superpowers、`.superpowers`、`docs/superpowers` 相关工作流、技能、目录、模板或产物。不得恢复、引用或继续执行历史对话中生成的 Superpowers 内容。


## 每轮修改后的手动复核

- 每次完成代码修改、功能调整或界面打磨后，必须主动启动或更新运行中的 Atlas，并打开本轮修改所涉及的最新界面，供用户手动复核，不得等待用户再次提醒。
- 确认打开的界面实际运行本轮最新代码；不能只报告类型检查、测试或构建通过，也不能以旧安装包、截图或静态预览代替可操作的最新界面。
- 在交付说明中简要指出已打开的页面、本轮变化和建议用户复核的操作路径。涉及多个页面时，说明其他受影响页面的入口。
- 打开和展示界面时，不得擅自替用户执行迁移、删除、清空、恢复等真实数据操作；需要演示执行结果时使用隔离测试资料库，并明确告知用户。
- 若界面启动或自动打开受权限、工具或环境限制，应先完成可行的验证，再明确说明具体阻碍及最短的手动打开方式；不得声称已打开或已完成手动复核。

## UI 设计强制约束与交付门禁

以下要求适用于所有新增或修改的页面、弹窗、表单、修复流程、成功状态和错误状态。功能可用不等于设计合格；不得以“后续再打磨”为由交付已知不符合规范的界面。

### 1. 实现前必须核对设计来源

- 必须先阅读 `design.md` 中与本次界面有关的章节，并核对 `src/styles/tokens.css`、`src/styles/global.css`、`src/components/ui/` 及同类已验收页面。不能仅凭记忆、截图印象或个人审美开始写页面 CSS。
- 设计事实优先级固定为：tokens → global CSS → 公共 UI 组件 → 页面局部布局 → 可执行设计合同与视觉验证 → design.md。文档与实现冲突时必须查明原因，不得挑选更方便的一方绕过约束；已知不合规的旧页面也不能作为照抄依据。
- 开始实现前，必须明确本轮复用的页面轨道、组件、字号角色和间距角色，并选定同类界面作为对照。修复工具和临时补录页面不享有豁免。

### 2. Token 与公共组件必须真正复用

- 颜色、表面、文字层级、字号、行高、字重、间距、圆角、控件高度、阴影和动效必须使用现有语义 token；禁止凭空创造近似值、复制一套局部主题或为单页增加同义 token。
- 使用 `var(...)` 只是必要条件。必须同时符合 token 的语义角色和页面族规则；任意叠加多个合法间距、用错误文字角色或把所有说明铺开，仍属不合规。
- 优先复用 `src/components/ui/` 的按钮、字段触发器、日期选择器、状态和弹窗组件；不要用裸 HTML 控件加零散样式拼出第二套视觉语汇。确无对应公共组件时，必须沿用已有控件合同及交互状态。
- 页面使用已有内容轨道；弹窗使用 `ModalShell` 已有尺寸与结构。不得为容纳冗余内容擅自增加宽度档位、放大弹窗或修改全局 token。
- 局部 CSS 只补充必要的业务布局，不得覆盖公共组件的内边距、标题字号、按钮高度等合同来掩盖布局问题。新增 token 或修改公共合同前，必须说明现有方案为何无法表达，并验证受影响的其他页面。

### 3. 信息必须克制、渐进呈现

- 每一步只呈现完成当前决策所需的信息。输入、预览、完成应有明确边界，不得把完整表单、规则复述、结果说明、全部明细和审计资料同时堆在一个弹窗里。
- 同一事实只保留一个主要表达位置。标题说明、辅助文案、摘要、结果区不得重复解释同一条规则或“保持不变”的事项；必要的影响说明集中在确认步骤。
- 预览优先呈现适用区间、变更摘要、关键前后对比和确认动作。交易明细默认使用“查看 N 笔交易”等按需展开入口；不得把长列表作为默认主体。
- 审计历史默认收起；使用用户能识别的日期、规则摘要或名称。内部 ID、UUID、数据库字段、版本号、原始异常栈只能放入按需展开的技术详情，不能出现在普通业务摘要中。
- 无待处理项时展示一个明确完成状态；不得同时堆叠“待处理 0 项”“历史缺口 0 项”“涉及 0 笔”和重复成功横幅。保留有价值的历史入口即可。
- 复杂流程必须用渐进呈现降低密度，不能用层层卡片、重复分隔线、额外标签或大段说明制造层级。

### 4. 间距、对齐与滚动必须可验证

- 行内、字段、区块和页面间距分别遵循现有角色，优先使用 `--space-row-gap`、`--space-section-gap`、页面 inset 和 `--modal-*-padding`；不得随意混用基础间距或叠加父级 gap、子级 margin、padding 造成双倍留白。
- 标题、摘要、表单、表格与操作区必须对齐同一内容起始边。先明确每一段间距由哪一层负责，再写样式；不得以空段落、空容器或换行撑开空间。
- 字号、行高、字重必须完整对应文字角色；必须核对最终 computed style 和实际几何尺寸，不能只检查 CSS 中是否出现 token 名。
- 普通弹窗只保留一个正文滚动容器，标题和操作区按公共组件合同处理。禁止在正文内再套固定高度、独立滚动的普通明细列表；大数据明细应转为独立查看步骤或现有专用列表容器。
- 确认所必需的勾选、说明与主操作应处于同一可理解的操作区域。不得让大量次要明细把必需确认项推到内层滚动底部，造成按钮看似无反应。
- 不得用放大窗口、降低缩放、缩小字体或只截取局部的方式掩盖拥挤、留白、遮挡和滚动问题。

### 5. 阻断必须有明确可见的解决路径

- 当前步骤只保留一个主操作，按钮文案说明下一步的具体动作；推荐值须明确来源、可修改并展示生效范围，不得替用户确认历史事实或默认勾选破坏性操作。
- 缺失字段应在字段附近显示校验信息，并关联可访问说明、聚焦首个需处理字段；错误不能只出现在长表单底部或短暂 Toast 中。
- 无副作用的预览不得被仅在最终保存时需要的审计依据或确认项阻断；最终提交仍须执行完整校验。
- 禁用、失败、重试、取消必须有明确结果，保留已输入内容和上下文；技术错误应转换为用户能执行的下一步，原始错误仅放入详情。

### 6. 视觉验收是交付与打包前置条件

- 每次 UI 修改完成后，必须先自行检查最新可操作的 Windows/macOS 桌面界面，再请用户复核。用户手动复核不能代替开发者自检；类型检查、单元测试、DOM 文本断言和构建通过均不能代替视觉验收。
- 至少检查当前实际窗口及受支持的较小桌面窗口，并覆盖用户实际使用的显示缩放；以当前最小窗口约束为准，不以最大化窗口作为唯一证据。未验证的平台或缩放必须如实说明。
- 必须使用与问题相当的数据量及代表性长文本。用户暴露的是 10 笔记录，就至少覆盖 10 笔，不能用 1 笔短文案样例宣称通过。只使用隔离测试资料库演示真实写入。
- 必须覆盖本次涉及的默认、展开、输入、预览、校验失败、提交中、完成和空状态；检查布局溢出、滚动容器数量、对齐、文字层级、控件尺寸及必需操作的可达性。
- 同时保留完整界面截图和关键 computed style / geometry 验证结果，并与选定的同类界面对照。截图必须实际查看；生成图片或确认文件存在不等于完成视觉检查。
- 发现上述任一违反项，必须先修正再交付或打包；禁止把“功能已经可用”“测试已经通过”“使用了 token”作为放行理由。
- 交付说明必须简要说明复用的设计基线、已验证的数据量与状态、实际打开的最新页面和仍未覆盖的限制。只有文档修改时无需为了凑证据启动客户端，也不得声称相关页面已经修复。


### 7. 高频使用界面的减法门禁

- 默认界面只呈现当前数据、必要设置和主要操作。每段常驻说明必须回答“它会改变用户当前哪一个决定”；回答不出的删除，不得换成折叠面板继续堆积。
- 教学示例、实现机制、正常状态、跨平台无关说明不得常驻；必要帮助按需提供。异常必须保留原因和可执行的解决入口。
- 同一设置不得同时用眉题、标题、说明、提示卡和保存摘要重复表达。生效范围与不可逆后果只在最相关的操作位置完整说明一次，不得因精简而隐藏。
- 同类分类共用管理操作区，低频批量操作按需出现。移除重复装饰图标，保留图标按钮的可访问名称；悬停操作必须在键盘聚焦时同样可见。
- 信息减法不得改变数据含义、自动合并用户分类、取消原有能力或省略统计口径。不能靠小字、低对比度、挤压间距、扩大窗口或增加更多卡片冒充简洁。
- 每轮打磨必须检查同类页面是否存在相同问题，记录已检查范围及保留原因；不能只处理用户截图的局部。交付前复核默认状态的信息量，而不只是溢出和功能通过。
