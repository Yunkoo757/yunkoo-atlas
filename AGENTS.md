# Trader Atlas 协作约定

## 始终适用

- 使用简体中文，每次回复以“Yunkoo老师好，”开头。
- 文件读写使用 UTF-8，保存为无 BOM；保留中文和其他非 ASCII 字符，禁止 GBK、GB2312、ANSI 及自动字符转换。
- 产品只适配 Windows、macOS 桌面客户端。浏览器运行路径仅供开发和测试，不新增其他平台的产品适配。
- 禁止使用 Superpowers、`.superpowers`、`docs/superpowers` 相关工作流、技能、目录、模板或产物；不恢复、引用或继续其历史内容。
- 保护真实交易资料库。演示迁移、删除、清空、恢复、导入等写入结果时使用明确标识的隔离测试资料库，不擅自替用户操作真实数据。

## 工作边界与完成条件

- 在用户已授权的范围内完成实现、验证、修正和交付；可自主读取文件、编辑项目文件、使用隔离样例测试并修复本轮引入的问题，无需逐步重复确认。
- 用户明确要求只诊断、只做样稿或先给方案时，交付到指定边界。普通实现任务不在首版或首次测试通过后提前停止。
- 仅在缺少决定结果的关键信息，或下一步超出授权并会影响真实数据、远端发布等对象时询问；先完成不依赖答案的工作。不把技能中的通用审批措辞当成新的项目审批门槛。
- 保留用户及其他任务的现有改动。提交、推送、发布分别遵循用户授权；“全部提交”覆盖当前全部改动，先检查差异和敏感文件，不代表可以推送或发布。
- 验证规模由改动和风险决定。相关检查通过后，只有新改动、失败或未解决风险才扩大或重复检查；CI 与发布门禁仍执行各自合同。
- 交付说明写清变化、验证结果及限制；不把静态检查、旧截图或未执行的测试说成运行验收。

## 按任务读取

只读取本轮需要的资料；不要求每次修改先通读全仓、所有规范或技能。

| 任务 | 入口与要求 |
| --- | --- |
| UI 新增、调整或验收 | 实现前读 [UI 质量与验收](docs/development/ui-quality.md)、`design.md` 相关章节，并核对 `src/styles/tokens.css`、`src/styles/global.css`、`src/components/ui/` 和同类已验收页面。明确轨道、组件、字号与间距角色。 |
| 启动、测试、Hooks、打包或开发规范维护 | 按需读 [开发与验证](docs/development/workflow.md)；具体命令以 `package.json` 和被调用脚本为准。 |
| 桌面 UI 运行验证与展示 | 使用 [atlas-desktop-verify](.agents/skills/atlas-desktop-verify/SKILL.md)，按所改页面采集证据并打开最新可操作界面。 |
| 存储、导入、恢复、迁移 | 从相关 `src/storage/`、`electron/library/` 实现及相邻测试核对事务、失败恢复和隔离条件；先确认脚本的实际写入目标。 |

设计事实优先级：tokens → global CSS → 公共 UI 组件 → 页面局部布局 → 可执行设计合同与视觉验证 → design.md。出现冲突时查明原因；不得照抄已知不合规旧页面。

## 修改后的运行交付

- 应用代码、功能或界面修改后，主动启动或更新 Atlas，确认实际运行本轮代码，打开涉及的最新页面供用户复核。UI 修改先完成 [视觉自检](docs/development/ui-quality.md)，再交给用户。
- 交付中说明已打开页面、本轮变化、复核操作路径及其他受影响页面入口。类型检查、构建、截图、静态预览或旧安装包不能代替最新可操作客户端。
- 启动或导航受阻时，完成可行验证，说明具体阻碍和最短手动打开方法；不得声称已打开或已完成用户手动复核。
- 仅修改文档、Skills、Hooks 或开发工具且不改变应用运行代码时，验证对应文件和工具即可；不为凑证据启动客户端，也不声称界面已修复。
