# Trader Atlas 设计规范

> 状态：当前实现基线
> 更新日期：2026-09-05
> 适用平台：Windows 客户端、macOS 客户端

## 1. 文档定位

本文档记录 Trader Atlas 当前软件已经形成的产品设计语言、桌面布局、组件规则和交互约束，是后续新增功能与界面调整的设计基线。

设计事实的优先级如下：

1. `src/styles/tokens.css`：颜色、字体、间距、圆角、尺寸、层级、阴影与动效的唯一令牌来源。
2. `src/styles/global.css`：全局排版、输入、焦点、选择、滚动条与减少动效规则。
3. `src/components/ui/`：通用桌面组件及其状态定义。
4. 各页面和业务组件的 CSS：在语义令牌与通用组件无法完整表达时提供局部布局。
5. `*.design.test.ts`、浏览器测试和桌面视觉检查：可执行的设计合同。
6. 本文档：对上述实现的统一解释与使用指南。

`docs/` 中按日期保存的设计稿属于方案与决策记录。方案只有在进入现有代码、令牌和设计测试后，才成为本文档所描述的当前规范。

## 2. 平台边界

Trader Atlas 是本地优先的桌面交易日志与复盘工作台，后续只适配：

- Windows 桌面客户端。
- macOS 桌面客户端。

不为手机、iPad、浏览器或其他平台新增响应式设计、触控专用流程或兼容方案。代码中存在的浏览器运行与测试路径仅用于开发、回归和技术验证，不构成产品交付承诺。

桌面设计默认：

- 键盘与鼠标共同使用，常用操作应有快捷键路径。
- 信息密度高于消费型应用，但不能牺牲层级和可扫描性。
- 窗口缩窄时优先收紧侧栏与内容宽度，不转换为移动端布局。
- 平台快捷键显示遵循系统习惯：Windows 使用 `Ctrl`，macOS 使用 `Command`。
- 原生关闭、托盘、更新与窗口行为可按系统分别处理；业务页面的视觉语言保持一致。

## 3. 产品设计目标

Trader Atlas 帮助交易者持续完成四件事：

1. 低摩擦记录交易、计划、模拟盘和错过机会。
2. 在盘后补充事实、截图、结果与复盘正文。
3. 把高价值内容沉淀为可追溯的案例。
4. 通过统计、周复盘、随机复盘与风险证据形成反馈闭环。

设计不追求“金融终端式炫技”，也不把所有业务状态做成高饱和仪表盘。界面应像一张安静、可靠、可长时间使用的专业工作台：默认克制，只在盈亏、风险、等待和关键操作上使用语义色。

### 3.1 核心原则

- **事实优先**：交易事实、结果、风险证据和来源关系比装饰性指标更重要。
- **先捕捉，后整理**：首次记录减少必填判断，完整信息可在详情和复盘阶段补齐。
- **层级而非堆卡片**：优先用间距、排版、分隔线和表面层级组织内容；卡片只用于真正独立的对象或操作单元。
- **语义色稀缺**：颜色代表业务含义，不作为大面积装饰。
- **连续上下文**：创建、编辑、筛选和复盘完成后尽量保留用户原来的页面、范围与焦点。
- **渐进呈现**：高级筛选、次要属性、历史和修复工具按需出现。
- **数据可信**：统计口径、范围和缺失状态必须可解释，不用漂亮图表掩盖数据边界。
- **提醒不替用户决策**：风险界面强化证据、确认与审计，不以视觉恐吓代替交易判断。

## 4. 信息架构

当前应用启动后进入交易日志 `/list`。桌面外壳由固定侧栏、内嵌主内容框和全局浮层组成。

### 4.1 侧栏

侧栏当前宽度为 `244px`；窗口宽度低于 `1100px` 时收紧到 `208px`，折叠状态为 `56px`。侧栏包含：

- 账户入口，以及设置和回收站菜单。
- 搜索/命令面板入口。
- 随当前上下文变化的新建入口。
- “工作台”主要导航。
- “我的空间”中的固定项、保存视图、策略来源和可管理溢出项。
- 历史实盘入口。

当前页面即使已从“我的空间”隐藏，也要以“当前页面，已隐藏”的幽灵项保留位置感。侧栏项目支持管理和排序，但不得生成干扰阅读的浏览器原生拖拽预览。

### 4.2 主要内容域

主内容域与窗口四周保留 `8px` 内嵌间距，使用 `12px` 圆角、细边框和轻阴影，与应用背景形成桌面窗格关系。

稳定内容宽度分为四档：

| 令牌 | 宽度 | 用途 |
| --- | ---: | --- |
| `--page-rail-wide` | `1240px` | 仪表盘、高密度综合页面 |
| `--page-rail-standard` | `1180px` | 列表、设置总览、常规工作台 |
| `--page-rail-reading` | `920px` | 周复盘、阅读与证据页面 |
| `--page-rail-form` | `680px` | 表单、单列设置与专注编辑 |

页面不得随意新增第五种最大宽度。确有新布局需求时，先证明四档内容轨道无法表达，再更新令牌与设计合同。

交易日志、案例记录和回收站属于数据工作台，应占满主内容窗格的可用宽度；它们不属于第五种内容轨道，也不得被强行套入 `wide` 或 `standard` 居中轨道。设置页按内容角色显式选择单列表单或“主区 + 辅助区”，并从内容起始边对齐，不因宽窗口产生漂移。

### 4.3 当前核心页面族

- **交易工作台**：交易日志列表、看板、进行中、星标、周期、策略和不同交易范围。
- **日常记录**：今日工作台、随记、模拟盘、错过机会。
- **复盘体系**：案例记录、随机复盘会话、周复盘。
- **分析与历史**：仪表盘、历史实盘。
- **对象详情**：交易详情、案例来源、富文本与图片查看。
- **系统管理**：个人资料、快捷键、策略、风险、标签、品种、模板、显示、数据和更新。
- **恢复与维护**：回收站、导入数据健康、风险数据修复、阶段归属修复。

旧路由重定向用于兼容历史入口，不应在视觉上生成第二套页面体系。

## 5. 视觉语言

### 5.1 色彩基调

应用采用中性微靛暗色轴。背景之间依靠低对比明度差形成层级，避免纯黑与纯白的强烈割裂。

四层桌面表面：

| 语义 | 令牌 | 使用场景 |
| --- | --- | --- |
| 应用底层 | `--surface-app` | 窗口背景、侧栏背景 |
| 页面窗格 | `--surface-pane` | 主内容框、页面主体 |
| 内嵌表面 | `--surface-inset` | 评分格、证据区、局部输入区 |
| 浮动表面 | `--surface-floating` | 菜单、弹窗、命令面板、提示 |

状态与业务色：

| 令牌 | 含义 | 规则 |
| --- | --- | --- |
| `--accent` | 品牌、选中、主要操作、键盘焦点 | 当前基准为 `#5e6ad2`，不可当普通装饰色铺满页面 |
| `--pos` | 盈利、完成、正向结果 | 仅表达业务正向状态 |
| `--neg` | 亏损、错误、危险操作 | 取消不等同于亏损，不得混用 |
| `--warn` | 警告、需要注意 | 不承担普通待处理状态 |
| `--pending` | 等待、待处理 | 比错误与警告更弱 |
| `--opportunity` | 错过机会 | 不进入真实盈亏视觉体系 |
| `--star-color` | 星标 | 星标金与置顶、警告色分开 |

导航图标默认保持灰阶；选中态以统一的高亮文字、图标和背景表达当前位置。允许少量受控的选中图标语义色帮助识别固定入口，但颜色必须来自统一映射，不能改变文字或背景的选中层级，也不能形成彩虹式噪声。

### 5.2 文字层级

文字使用角色而不是任意灰度：

- `--text-strong`：页面标题、关键对象、最高优先级内容。
- `--text-body`：正文、主要说明、常规可读信息。
- `--text-muted`：元数据、辅助说明、次级标签。
- `--text-faint`：最低优先级但仍需阅读的提示。
- `--text-disabled`：仅用于不可交互或不可用状态，不承载正常内容。

不得用 disabled 灰度表现普通次要信息，也不得只通过降低透明度隐藏重要内容。

### 5.3 字体与数字

界面字体栈以 Inter Variable 承载拉丁和数字；中文由桌面系统字体承载，macOS 优先使用苹方，Windows 优先使用微软雅黑，随后回退到其他系统无衬线字体。当前不内置统一的中文字体文件，因此两个桌面平台允许保留各自原生的中文栅格化表现。正文和界面默认不使用等宽字体。

字体角色：

| 角色 | 规格 | 用途 |
| --- | --- | --- |
| 页面标题 | `20px / 28px / 600` | 页面最高层标题 |
| 弹窗标题 | `15px / 600` | 模态框标题 |
| 区块标题 | `13px / 600` | 分区、卡片或列表组标题 |
| 导航 | `14px / 20px / 450`，选中 `500` | 侧栏与设置导航 |
| 列表主项 | `13px / 20px / 500` | 品种、主要对象与列表强信息 |
| 列表次项 | `13px / 20px / 450` | 编号、策略来源与列表辅助信息 |
| 数据行 | `13px / 20px / 400` | 按钮、表格与常规高密度行 |
| 正文 | `15px / 23px / 400` | 说明、编辑器与长文本；应用框架和高密度界面仍使用数据行或列表角色，不以正文规格覆盖全局 |
| 标签 | `12px / 18px / 450` | Chip、筛选标签与短状态 |
| 元数据 | `12px / 18px / 400` | 时间、标签说明、辅助信息 |
| 标注 | `11px / 16px` | 极短标签、计数与技术标注 |
| 财务值 | `20px / 1.15` | 关键盈亏或风险数值 |

只有快捷键键帽、代码、错误码、原始数据与文件名预览使用 `--font-mono`。金额、百分比、R 倍数、时间和表格数字使用 `tabular-nums` 保持列对齐。

禁止新增未经批准的 `700` 字重、夸张字距或大号营销式标题。

### 5.4 间距与密度

基础间距采用 4px 阶梯：`4 / 8 / 12 / 16 / 20 / 24 / 28 / 32px`。常用规则：

- 同一控件内部图标与文字：`6–8px`。
- 同类数据行之间：`8px` 或通过行高控制。
- 页面区块之间：`24px`。
- 页面水平内边距：紧凑 `20px`、默认 `28px`、宽松 `32px`。
- `6px`、`7px` 等非阶梯值只用于已经命名的光学校准，不扩散为新体系。

交易日志、案例记录和回收站共享列表密度：默认“紧凑”为 `44px`，用户可在显示设置中切换到“舒展” `48px`；虚拟列表的估算高度必须与实际 CSS 行高使用同一密度值。月份条内容高度为 `36px`，其虚拟占位高度为 `44px`（上下各 `4px` 间距）；过滤栏为 `43px`。高密度不等于缩小所有点击目标；优先通过稳定行高、对齐和内容裁切维持扫描效率。

### 5.5 圆角、边框与阴影

- 常规控件：`6px` 或 `8px` 圆角。
- 浮动菜单：`10px` 圆角。
- 主内容窗格：`12px` 圆角。
- 模态框：`14px` 圆角。
- 胶囊或圆形操作：仅使用 `--radius-full`。

边框优先使用 `--border-subtle`、`--border-default`、`--border-strong`。阴影只用于表达真实抬升：主窗格、浮层、弹窗和拖拽对象。普通卡片不得通过多层阴影制造无意义的悬浮感。

## 6. 布局与页面构成

### 6.1 页面骨架

常规页面从上到下由以下区域按需组合：

1. 页面标题/面包屑与主操作。
2. 视图切换、范围或筛选工具栏。
3. 风险、数据健康或流程状态条。
4. 主内容：列表、看板、图表、编辑器或表单。
5. 仅在选中对象时出现的批量操作条或局部浮层。

同一页面只保留一个明确主操作。主要按钮不应同时出现在标题栏、内容卡片和页面底部重复争夺注意力。

### 6.2 列表

交易列表是主要高密度视图：

- 列标题和数据行必须共享同一网格模板。
- 数字右对齐或按表格数字特性对齐。
- 月份/分组标题可吸附，但不能遮挡列标题。
- 展开收起通过箭头旋转与行高平滑变化表达。
- 分组状态通过三角和状态图标着色，不整条染色。
- 行选择框在悬停、键盘焦点或选择模式中可见。
- 星标保持可发现，但不能压过品种、结果与状态。
- 大列表使用虚拟化时，视觉行高、吸附偏移和键盘语义必须保持一致。
- 虚拟列表使用一个复合 `list` 语义；分组标题和真实数据行都是该列表的 `listitem`，虚拟定位容器不得制造重复或断裂的可访问结构。

### 6.3 看板

看板用于状态流转和案例分类，不是默认统计视图：

- 常规列宽 `272px`，案例列可收紧到 `260px`。
- 列之间使用轻边框和低对比底色，不做大块高饱和状态背景。
- 拖入目标列时用 accent 边框与轻底色反馈。
- 卡片内容顺序为对象标识、主要标题、策略/标签、结果或辅助数值。
- 案例卡片末尾提供一行现有正文摘要，优先使用正文标题；案例列表通过名称悬停或键盘聚焦预览摘要，维持原有行高。
- 拖拽卡片降低透明度并轻微缩放，插入位置用明确的 accent 指示线。

### 6.4 详情与编辑

交易详情优先呈现对象身份、截图、复盘正文和关键结果。属性、标签、时间与来源关系放在稳定属性区，避免正文被表单网格包围。

标题下以低对比背景行展示记录日期、周期、结果和记录类型，收起属性区后仍保留阅读上下文。属性区先呈现核心属性与交易数据；交易时段、心理状态和市场叙事在补充信息中固定顺序逐行呈现，使用单层分组折叠，不按填写状态移动字段。复制编号统一从顶栏“更多”进入，不占用属性区独立卡片。复盘起稿使用公共 ghost 按钮，与标题下背景信息同行，空间不足时自然换行。

属性区可从顶栏展开或收起，宽窗口记住显示偏好；不超过 `1200px` 的桌面窗口使用临时属性抽屉。标签分组展开后直接显示已选标签与未选预置候选，支持一键添加；分组折叠收纳。完成复盘入口固定在顶栏，完成后在同处提供下一条待复盘入口，提示消息只承担结果反馈。

顶栏将复盘状态与推进动作组成一组，再与记录序号及前后导航区分。完成操作与完成后的“下一条待复盘”均使用公共中性轻边框小按钮；待复盘时仅显示完成操作，不重复显示待复盘标签；完成后显示已复盘状态与下一条入口。自动保存成功保持安静，不占据顶栏；未保存或保存中持续超过 600ms 才显示，失败立即呈现并保留重试入口。直接跳转不附加下拉箭头。

详情页默认以 `Tab` 切换属性区，可在“交易 / 案例详情”快捷键分类中修改或禁用。输入框、编辑器、菜单和属性抽屉内保留焦点导航，`Shift+Tab` 保持反向导航。列表 / 看板、详情与随机复盘分别限定作用域，允许复用按键；与普通页面同时生效的全局 / 导航动作参与跨范围冲突检查。

富文本正文使用 UI 字体、`15px / 23px` 正文角色；代码节点单独保留等宽字体。粘贴内容不能把外部字体带入可见正文。图片支持预览、灯箱、缩放和平移，加载失败必须有可理解的恢复或错误状态。

灯箱在同一次图片组查看中分别记住每张图的缩放与平移，关闭后清除。随机复盘多图支持单图大图与两图对照，说明可单独收起；布局切换不修改原始图文内容。会撤销掌握度评估的返回操作明确命名为“撤销上次评估并返回”。

### 6.5 仪表盘与周复盘

- 仪表盘先交代当前数据范围，再展示结论和图表。
- 三项核心指标在桌面窗口保持同排，策略区随窗口收紧列宽。
- 图表颜色复用业务语义，不建立独立彩色调色盘。
- 风险、趋势和审计内容以证据连续性为优先，不使用阴影、发光或渐变制造严重程度。
- 周复盘采用阅读轨道；普通区块保持透明，通过标题、间距和分隔组织，只有成对对照或风险证据等独立内容使用抬升表面。

### 6.6 空、加载、错误与恢复状态

状态必须区分：

- **首次空白**：解释该区域是什么，并给出创建入口。
- **筛选无结果**：保留筛选上下文，主操作是重置或调整条件。
- **任务已完成**：用中性或正向反馈说明没有剩余事项。
- **对象缺失**：说明对象已删除、来源不可用或链接失效，并提供返回路径。
- **加载中**：延迟显示，避免快速路由切换闪烁；减少动效时提供静态状态。
- **页面异常**：保留错误边界、错误码和安全恢复动作。
- **存储恢复**：优先保护数据，清楚说明重试、恢复和退出的后果。

空状态不能把过滤无结果误写成“还没有记录”，也不能用插画取代恢复动作。

## 7. 组件规范

新增页面优先复用 `src/components/ui/` 中的组件。

### 7.1 按钮

可交互按钮语汇固定为四种：

- `ghost`：工具栏、低优先级或无损操作。
- `bordered`：次级确认、取消、独立但非主要操作。
- `primary`：弹窗确认或极少数核心路径 CTA。
- `danger`：危险文字操作。

持续状态不伪装成第五种按钮；使用状态条、Badge 或只读文字表达。最终且高风险的破坏性确认仍使用 `danger` 语义，并通过确认层级提升强度。

轻边框按钮使用单层实体边框，默认与悬停均不叠加外圈阴影，颜色继续使用 `--surface-control-border` 与 `--surface-control-border-active`。有明确文字的操作不重复弹出同文案提示；仅在需要解释受阻原因时提供提示。

尺寸为 `28px`、`32px`、`36px`。按钮按下可下移 `1px`，禁用态必须同时通过颜色、光标和可用性表达。一个操作不能仅靠图标颜色区分危险与普通状态。

### 7.2 图标按钮与图标

图标使用 Lucide 线性图标，消费 `14 / 16 / 18 / 20 / 24px` 五档光学尺寸。默认使用 `16px`，装饰性图标应隐藏于无障碍树。

只有图标的按钮必须有可访问名称，并在需要时提供快捷键提示。图标按钮不能依靠 hover 才暴露其唯一含义。

### 7.3 Chip、状态与标签

Chip 高度为 `20px` 或 `24px`，分为 default、soft、outline。Chip 用于短标签、状态、筛选值，不承载完整句子或主要操作。

业务状态必须使用统一映射；错误标签的强度高于时段标签，普通标签最弱。状态除颜色外还应有文本、图标或形状线索。

### 7.4 表单

表单控件基准高度 `32px`，字段水平内边距 `10px`。默认、悬停、聚焦、禁用状态统一消费 `--field-*` 令牌。

- 标签始终说明字段含义，placeholder 只提供示例或格式提示。
- 校验错误贴近字段，保留用户已输入内容。
- 长表单按业务阶段分组，次要字段渐进展开。
- 日期、选择器和触发器使用共享组件，不用原生样式拼出平行视觉。
- 危险操作与普通保存操作保持物理间隔。

### 7.5 菜单、弹出层与命令面板

菜单使用浮动表面、轻边框、`10px` 圆角和统一阴影。菜单项高 `30px`，危险项保持红色语义。命令面板最大宽度 `640px`，位于窗口上方约 `14vh`，结果行高 `38px`。

浮层必须处理：

- 打开后的初始焦点。
- 方向键、Enter、Escape 和 Tab 行为。
- 点击外部关闭。
- 视口边缘碰撞。
- 关闭后焦点返回触发元素。
- Portal 中继续使用统一字体和 z-index。

### 7.6 模态框

模态框尺寸：紧凑 `440px`、默认 `560px`、宽 `720px`，最大高度不超过窗口减 `32px`。结构固定为标题、可滚动正文、操作区。

确认操作放在右侧；取消在确认之前。破坏性确认使用危险语义，不以标题或整块背景大面积染红。模态框必须有焦点圈定、Escape 策略、关闭后焦点恢复和减少动效支持。

### 7.7 Toast 与就地状态

- 短暂成功且不需要决策：Toast。
- 字段问题或当前区域失败：就地状态。
- 会阻断继续操作且需要选择：模态框。
- 持续风险或数据健康信息：页面状态条。

成功 Toast 不抢占焦点；错误提示不能自动消失到用户来不及阅读。

## 8. 交互与动效

### 8.1 状态反馈

所有可交互控件至少定义默认、hover、active、focus-visible、disabled；可选择控件还要定义 selected，异步控件还要定义 busy。

Hover 只做轻微背景或文字提升。Active 可使用 `1px` 位移或轻微亮度变化。Selected 表达持续状态，不能与短暂 hover 使用完全相同的表现。

### 8.2 动效

动效按意图使用：

- 状态变化：`100ms`。
- 菜单：`100ms` 左右。
- 弹窗进入：`150ms` 左右。
- 弹窗退出：`120ms` 左右，略快于进入。
- 面板和布局变化：`220ms`。

统一使用已有 ease-out、ease-in 和 ease-in-out 曲线。动效只解释空间关系、状态变化和操作结果，不做持续漂浮、发光或无意义弹跳。

当系统启用 `prefers-reduced-motion: reduce` 时，加载动效、浮层进入退出和其他非必要动画必须移除或替换为静态反馈。

### 8.3 键盘与快捷键

- 全局搜索/命令面板和高频新建操作应可通过快捷键触发。
- `Tab` 顺序跟随视觉与业务顺序，不使用正 `tabindex` 人为改序。
- `Escape` 关闭最上层可关闭浮层，不穿透触发底层操作。
- 列表、菜单、选择器与命令面板使用与其 ARIA 模式相符的方向键行为。
- 快捷键展示使用 `Kbd` 组件及等宽字体，并按 Windows/macOS 显示正确修饰键。

### 8.4 拖拽

拖拽用于看板状态调整、侧栏空间管理、模板和品种排序。必须同时提供：

- 明确拖拽手柄或可拖区域。
- 拖拽中、合法目标和插入位置反馈。
- 禁用状态。
- 不依赖拖拽的可访问替代路径；重要排序至少可通过菜单或按钮完成。

## 9. 可访问性

### 9.1 焦点

不提供键盘焦点高光、定位描边及相关显示设置。保留业务快捷键、输入框编辑状态和弹窗焦点管理；Tab 属性切换不得激活侧栏或列表的焦点高亮。

### 9.2 语义

- 页面保持唯一、可理解的主标题。
- 导航使用 `nav` 和可辨别标签，当前项使用 `aria-current`。
- 原生数据表格提供 `row`、`columnheader` 等结构语义；交易日志、案例记录和回收站等虚拟化复合列表遵循第 6.2 节，统一使用一个 `list` 容器，并让分组标题和真实数据行使用 `listitem`，不得为了模拟表格制造重复或断裂的可访问结构。
- 折叠控件提供 `aria-expanded`。
- 弹窗、菜单、状态和进度使用对应角色，不用可点击 `div` 代替按钮。
- 图标按钮、关闭按钮、拖拽手柄必须有名称。

### 9.3 颜色与文字

颜色不是唯一状态线索。正常可读内容不得使用 `--text-disabled`。正文允许选择和复制；应用其他区域默认按桌面软件禁用文本选择，避免误选。

## 10. 内容与文案

- 使用简体中文，短句、动词优先，避免内部技术术语。
- 按钮写具体动作，如“保存记录”“恢复记录”，不用模糊的“确定”。
- 删除、覆盖、导入、恢复和阶段切换需明确对象与后果。
- 风险文案陈述事实和证据，不使用羞辱、恐吓或保证收益的语言。
- 盈亏、统计和候选数量说明数据范围，不能省略关键口径。
- 空状态先解释原因，再给下一步；错误状态说明哪些数据仍然安全。
- 表格、列表和统计中的可见空值统一显示为长破折号 `—`；不得用空白、`null`、`undefined` 或仅靠布局占位表达缺失。
- 同一业务概念在导航、标题、筛选和对话框中使用相同名称。

## 11. 设计治理

### 11.1 新功能落地顺序

1. 明确对象、用户目标、入口、完成状态和数据范围。
2. 选择现有页面轨道与通用组件。
3. 从语义令牌组合界面，不在页面中创建平行色板或尺寸体系。
4. 补齐键盘、空状态、错误、加载、禁用和减少动效。
5. 为稳定的视觉和交互约束增加设计测试或浏览器测试。
6. 运行类型检查、相关测试与桌面视觉治理检查。
7. 如果规范发生变化，同步更新 `tokens.css`、共享组件、测试和本文档。

### 11.2 禁止事项

- 页面直接建立新的品牌色、盈亏色或灰阶体系。
- 用硬编码颜色绕过语义令牌；品牌资源、外部图标等有明确理由的例外除外。
- 为单个页面新增近似重复的按钮、选择器、弹窗或状态组件。
- 用整行高饱和背景表达普通状态。
- 用阴影、渐变、发光和大圆角堆叠制造“高级感”。
- 把普通内容降到 disabled 对比度。
- 在 hover 中隐藏必要操作且没有键盘发现路径。
- 只验证默认有数据状态，遗漏空、错、慢、禁用和窄窗口状态。
- 为非目标平台引入响应式分支或交互妥协。

### 11.3 例外流程

若现有令牌或组件无法表达新需求，变更说明应包含：

- 现有语义为什么不足。
- 新语义是否会被至少两个场景复用。
- 与已有令牌和组件的边界。
- 默认、交互、禁用、错误和减少动效状态。
- Windows 与 macOS 的差异。
- 对应的自动化设计合同。

局部一次性需求优先留在组件作用域；稳定且跨页面复用的需求才提升为全局令牌或通用组件。

## 12. 验收清单

提交任何视觉或交互变更前，至少确认：

- [ ] 只面向 Windows 和 macOS 桌面客户端。
- [ ] 使用现有表面、文字、业务色、间距、圆角和动效令牌。
- [ ] 页面宽度属于四档内容轨道之一。
- [ ] 页面只有一个明确主操作。
- [ ] 默认、hover、active、focus-visible、disabled 和 busy 状态完整。
- [ ] 空、无结果、加载、错误和恢复路径与实际原因一致。
- [ ] 键盘可以到达并完成主要流程，关闭浮层后焦点正确返回。
- [ ] 状态不只依赖颜色表达。
- [ ] 关键数字使用表格数字，正文未误用等宽字体。
- [ ] 窄桌面窗口下没有遮挡、不可达操作或横向内容丢失。
- [ ] `prefers-reduced-motion` 下仍有清楚的静态反馈。
- [ ] 新增稳定规则已有设计测试或浏览器测试。
- [ ] 中文文本按 UTF-8 无 BOM 保存且未发生字符转换。

## 13. 关键实现索引

| 领域 | 文件 |
| --- | --- |
| 设计令牌 | `src/styles/tokens.css` |
| 全局交互与排版 | `src/styles/global.css` |
| 桌面应用外壳 | `src/components/ui/AppFrame.tsx`、`AppFrame.css` |
| 侧栏 | `src/components/Sidebar.tsx`、`Sidebar.css` |
| 通用控件 | `src/components/ui/` |
| 菜单与命令面板 | `src/components/Menu.*`、`src/components/CommandPalette.*` |
| 交易列表 | `src/components/trades/TradeList*` |
| 看板 | `src/views/BoardView.*` |
| 交易详情与编辑器 | `src/views/DetailView.*`、`src/editor/Editor.*` |
| 仪表盘与复盘 | `src/views/Dashboard.*`、`WeeklyReviewView.*`、`ReviewSessionView.*` |
| 设置 | `src/views/settings/` |
| 设计合同 | `src/**/*.design.test.ts`、`src/lib/designAudit*.test.ts` |
| 桌面视觉检查 | `scripts/check-desktop-visual-governance.mjs`、`scripts/qa-desktop-visual.mjs` |


## 14. 跨页面一致性收敛（2026-09-06）

- 周复盘页签、风险周期单选与普通分段切换共用 SegmentedControl；分别保留 tablist、radiogroup、group 语义及方向键导航。
- 分段控件与范围触发器使用单层外边界；分段选中项使用已有边框 token 的内描边，不叠加外圈阴影。
- 桌面窗口缩窄时不放大详情字段和标签高度；通过属性抽屉、列宽和换行维持布局。
- 设置新增、添加、上传使用次级按钮；开始任务及明确提交仍可使用 primary。筛选清除及返回使用次级操作。
- 待处理及草稿使用 pending；warn 仅用于实际风险提醒。
- 随记使用 reading 轨道；风险设置使用 form 轨道；图表跟随所属内容区。局部文本与图表的合理限宽不作为新的页面轨道。
- 详情进入编辑时消费 field 高度、内边距、圆角与编辑态颜色；添加标签使用公共 ghost 按钮，标签值与预置候选保持 Chip 语汇。
- 同级顶栏标题使用 toolbar 文字角色；区块间距使用已有阶梯。首次空白、筛选无结果与对象不存在保留各自的内容结构，统一间距及操作层级。

## 15. 官网与教学站：模块展示与动效规范（2026-09-06）

本节面向 Trader Atlas 桌面产品的官网与使用指南，不改变客户端工作流与桌面适配范围。

- 参考 TikHub 的背景响应、横向模块展示和叠层翻页构图；采用 Atlas 自有内容、截图与 SVG。保持安静、克制、低密度。
- 官网展示三层：首屏完整工作台；可横向浏览的九项功能模块；真实截图叠层画廊。随记保留独立介绍。任何功能都能进入对应指南。
- 模块轨道采用双行、低速自动往返滚动，速度 18 CSS 像素/秒；悬停、键盘进入、用户暂停、页面隐藏和离开视口时停止。保留原生滚动、前后按钮及键盘入口；不劫持纵向滚轮，不复制可聚焦内容。
- 画廊通过标签、前后按钮和方向键翻页，展示明确页码；当前截图以真实界面局部正视呈现，统一 16:9 固定视窗，后层仅提供空间关系。截图不成为链接，不提供下载、独立打开或放大入口。
- 鼠标背景仅在官网精细指针设备上响应，使用 Liquid Ether WebGL 流体背景，局限于首屏区域；按参考站配置使用蓝紫色调、力度 8、半径 80、黏度 60、透明度 .45，空闲 5 秒后恢复慢速演示；不替换系统光标、不尾随粒子、不干扰正文、控件和截图。离开页面及后台时停止更新。
- 首屏元件直接渲染最终占位。动效只使用 transform/opacity；不动画布局尺寸、margin、padding，不在首帧后补图标或重新居中。
- 教学站延续共享字体、颜色、边框、间距与控件；阅读正文不添加背景追光、翻页或滚动锁定。
- prefers-reduced-motion 下禁用追光与翻转，滚动立即定位；所有功能仍可用键盘和明确按钮操作。
- token 归属：atlas-docs-site/styles/brand.css 为两站共享视觉基础；其中 motion、glow、module、deck 角色仅供官网展示层消费。禁止在新组件中散落同义数值。
- 验证：轨道端点、九项功能链接、画廊七页标签/按钮/键盘同步、缩窄桌面窗口、减少动态效果、截图固定比例和刷新稳定性。
- 顶部胶囊使用固定宽度逐字输入/删除轮播；只展示真实功能文案。减少动态效果时保留静态首句，读屏名称不逐字播报，悬停及键盘聚焦时暂停。

- 流体实现来自 React Bits 的 LiquidEther，使用本地 Three.js 模块；保留上游许可。WebGL 不可用或减少动态效果开启时，降级为静态背景；离开首屏或标签页隐藏时暂停渲染。源码与许可位于 atlas-website/vendor。

### 15.1 流体可见性校准

官网首屏比参考站更高，流体范围限定到首张产品窗口上沿，避免自动演示落在图片背后。暗色输出采用 straight-alpha，只通过 alpha 衰减流体，消除颜色与透明度重复衰减。视觉增益 1.8、透明度 .32、力度 12、半径 100、黏度 45；空闲强度 .8，恢复等待 1800ms。参数统一在 brand.css。此组为 Atlas 可见性校准值，覆盖此前参考参数。

### 15.2 玻璃导航与截图构图

两站共享透明度 .62 的中性深色导航，24px 背景模糊，低对比细边框；尺寸和定位首帧固定，不随滚动变化。无 backdrop-filter 时回退纯色。首屏仅保留一次完整窗口（最大宽度 1120px）；功能画廊及随记介绍从原始 PNG 以 CSS 固定视窗裁取重点，减少重复侧栏和空白，保留真实文字。裁取不修改原始资产，不提供独立打开或下载控件。教学站保留完整窗口用于定位操作。截图视窗在组件加载前预留相同比例，翻页不改变页面高度。

### 15.3 局部截图像素密度

局部视窗统一保留 2160 像素宽的原始内容，常规桌面展示不超过 1000 CSS 像素，保证 2x 屏幕像素供应；2.25dppx 起限宽 720px，3.25dppx 起限宽 540px。密度规则使用 CSS 媒体查询，在首帧生效，不等待脚本测量后缩放。不得将窄裁片铺满大容器，也不以锐化或 image-rendering:pixelated 伪装清晰度。前景截图切换只淡入，避免透视变换导致文字临时发虚；后层保持装饰性叠放。

### 15.4 视觉审查收敛（2026-09-06）

现场审查覆盖官网功能轨道、截图画廊、随记介绍、场景与 FAQ，以及教学站随记章节。主要问题：小字与低对比叠加、灰色值分散、画廊标题/页签/媒体缺少共同对齐、连续 128px 区块间距机械重复、选中与悬停语义不统一。修正原则：

- 两站主轨道 1200px，共享导航边界；产品画廊与随记在图片清晰度允许的 1000px 轨道内统一对齐。禁止放大图片补齐排版。
- 正文 16px，导航及说明 14px，辅助信息 12px；通过正文、辅助和标题角色区分对比，不靠过小字号或过暗文字制造克制。
- 正文、辅助灰统一消费 atlas-body / atlas-muted。组件悬停仅中性色表面和边框变化；紫色保留给当前状态与品牌动效。
- 常规区块间距 96px，内容分组 40px；宽幅截图与章节阅读共用文字角色，各自保留必要功能密度。
- FAQ 展开符号与实际打开状态一致；章节选中项有浅表面反馈；前景截图保持正视及清晰度约束。

保留项：已有真实截图、九项功能、低速滚动、流体交互、文字聚焦、教学练习。后续素材空间：随机复盘原图留白过多、周期复盘原图尚未填写；要提升这两项的内容表现，需要另行采集适合展示的真实状态，不伪造结果。

### 15.5 展示素材与完整学习路径

展示素材统一使用最新 Electron 构建在隔离的 Yunkoo 示例资料库中实拍，4320×2880 PNG；原生渲染 3x 像素密度，禁止后期插值放大。示例交易、随记及周期复盘只用于演示，页面及图片替代文本明确标识。新版素材在 atlas-docs-site/assets/product/showcase，采集与示例脚本在 atlas-website。记录主窗口全貌，功能区按截图内容独立取景；首帧预留 3:2 全貌或 16:9 局部。最窄局部仍有 2160px，以既有显示密度上限约束。

画廊当前功能有对应的指南入口；随记介绍分为捕捉、整理、回看三步。图片保持展示语义，无放大、下载、独立打开入口。官网媒体与教学截图共用边框、阴影和圆角；教学搜索、练习与选择控件使用同一中性色表面。实际用户资料库不参与示例采集。
