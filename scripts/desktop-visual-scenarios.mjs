export const DESKTOP_VISUAL_VIEWPORTS = Object.freeze([
  Object.freeze({ width: 960, height: 640 }),
  Object.freeze({ width: 1280, height: 860 }),
  Object.freeze({ width: 1440, height: 900 }),
  Object.freeze({ width: 1600, height: 1000 }),
  Object.freeze({ width: 1920, height: 1080 }),
])

export const DESKTOP_VISUAL_SCENARIOS = Object.freeze([
  Object.freeze({ id: 'today', path: '/today-record', ready: '.trade-list' }),
  Object.freeze({ id: 'trades', path: '/list', ready: '.trade-list' }),
  Object.freeze({ id: 'detail', path: '/trade/TRD-131', ready: '.trade-detail-layout' }),
  Object.freeze({ id: 'dashboard', path: '/dashboard', ready: '.db-scroll' }),
  Object.freeze({ id: 'weekly', path: '/weekly-review', ready: '.wr-shell' }),
  Object.freeze({ id: 'review-session', path: '/review-session', ready: '.review-session-view' }),
  Object.freeze({ id: 'notes', path: '/notes', ready: '.quick-notes-page' }),
  Object.freeze({ id: 'missed', path: '/missed', ready: '.missed-view' }),
  Object.freeze({ id: 'review-cases', path: '/review-cases', ready: '.trade-list' }),
  Object.freeze({ id: 'paper-trades', path: '/sim', ready: '.list-scroll' }),
  Object.freeze({ id: 'live-archive', path: '/live-history', ready: '.live-archive-scroll' }),
  Object.freeze({ id: 'live-history-cases', path: '/live-history?view=cases', ready: '.list-scroll' }),
  Object.freeze({ id: 'trash', path: '/trade-trash', ready: '.trash-view' }),
  Object.freeze({ id: 'settings-profile', path: '/settings/profile', ready: '.profile-settings' }),
  Object.freeze({ id: 'settings-shortcuts', path: '/settings/shortcuts', ready: '.shortcuts-panel' }),
  Object.freeze({ id: 'settings-strategies', path: '/settings/strategies', ready: '.strategies-panel' }),
  Object.freeze({ id: 'settings-risk', path: '/settings/risk', ready: '.risk-management-settings' }),
  Object.freeze({ id: 'settings-risk-repair', path: '/settings/risk/data-repair', ready: '.risk-data-repair-view' }),
  Object.freeze({ id: 'settings-tags', path: '/settings/tags', ready: '.tag-presets-panel' }),
  Object.freeze({ id: 'settings-symbols', path: '/settings/symbols', ready: '.symbols-panel' }),
  Object.freeze({ id: 'settings-review-templates', path: '/settings/review-templates', ready: '.review-templates-panel' }),
  Object.freeze({ id: 'settings-display', path: '/settings/display', ready: '.display-settings' }),
  Object.freeze({ id: 'settings-data', path: '/settings/data', ready: '.data-settings' }),
  Object.freeze({ id: 'settings-updates', path: '/settings/updates', ready: '.update-settings' }),
])
